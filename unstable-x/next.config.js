/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["www.wargamer.com", "http://fishyhero.png"],
    },
};

module.exports = nextConfig;
